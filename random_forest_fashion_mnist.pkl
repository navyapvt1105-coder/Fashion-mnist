from sklearn.ensemble import RandomForestClassifier
import joblib

# Train Random Forest Classifier using your flattened training data
rf = RandomForestClassifier(n_estimators=100, random_state=42)
rf.fit(X_train_flat, y_train)

# Evaluate on test set (optional, you already have the evaluate_model function)
accuracy, precision, auc = evaluate_model(rf, X_test_flat, y_test, "Random Forest")

# Save the trained Random Forest model to a .pkl file for deployment
joblib.dump(rf, 'random_forest_fashion_mnist.pkl')

print(f"Random Forest model saved to 'random_forest_fashion_mnist.pkl' with accuracy: {accuracy:.4f}")
